// Explanations shown before the exercises. Keep the order aligned with the course levels.
window.LESSON_GUIDES = [
  {
    intro: 'База данных хранит сведения в таблицах. Таблица похожа на лист Excel: каждая строка — одна запись, каждый столбец — одно свойство записи. Например, в products одна строка описывает один товар, а name и price — его название и цену.',
    steps: ['SELECT указывает, какие столбцы показать. SELECT name, price выведет два столбца.', 'FROM указывает таблицу. FROM products означает «взять данные из products».', 'LIMIT ограничивает число строк. LIMIT 3 оставит три строки результата.'],
    pattern: 'SELECT столбец1, столбец2\nFROM таблица\nLIMIT число;',
    walkthrough: 'В примере SELECT name, city FROM users LIMIT 3: берём name и city из users и показываем три записи. SQL читает запрос сверху вниз, но сначала найдите таблицу после FROM, затем столбцы после SELECT.',
    practice: 'В задании про первые три товара «название» — это name, «цена» — price, «товары» — products, «первые три» — LIMIT 3. Подставьте эти части в шаблон и запустите запрос.'
  },
  {
    intro: 'Без условия SELECT показывает все строки таблицы. WHERE оставляет только те записи, для которых условие истинно. Условие относится к строкам, а не к названиям столбцов.',
    steps: ['Сравнение пишется после WHERE: age > 25, city = \'Москва\'.', 'AND требует выполнения обоих условий, OR — хотя бы одного.', 'IN проверяет значение по списку, LIKE ищет текст по шаблону: % означает любое количество символов.'],
    pattern: 'SELECT нужные_столбцы\nFROM таблица\nWHERE столбец = значение;',
    walkthrough: 'SELECT name FROM users WHERE age >= 18 выводит имена только тех пользователей, чей возраст не меньше 18. Текстовые значения берите в одинарные кавычки, числа — без кавычек.',
    practice: 'Сначала определите, какие столбцы нужны в результате. Затем найдите столбец, по которому нужно отобрать строки, и запишите условие после WHERE.'
  },
  {
    intro: 'Порядок строк в таблице сам по себе не гарантирован. Чтобы получить «самые дорогие» или «первых по возрасту», явно укажите сортировку.',
    steps: ['ORDER BY price сортирует по цене.', 'ASC означает от меньшего к большему; DESC — от большего к меньшему.', 'LIMIT берёт указанное число строк уже после сортировки. OFFSET пропускает первые строки результата.'],
    pattern: 'SELECT столбцы\nFROM таблица\nORDER BY столбец DESC\nLIMIT число;',
    walkthrough: 'В примере товары сначала упорядочиваются по price от высокой цены к низкой, затем LIMIT 3 оставляет три самых дорогих.',
    practice: 'Переведите слова «самый большой», «самый маленький» или «первые N» в ORDER BY с нужным направлением и LIMIT.'
  },
  {
    intro: 'Когда нужен итог по нескольким строкам, используют агрегатные функции. Они возвращают число вместо списка исходных записей.',
    steps: ['COUNT(*) считает строки; SUM(price) складывает значения; AVG(price) находит среднее.', 'AS задаёт имя результату: COUNT(*) AS total.', 'GROUP BY разделяет строки на группы, чтобы получить отдельный итог для каждого города или категории.'],
    pattern: 'SELECT группа, COUNT(*) AS total\nFROM таблица\nGROUP BY группа;',
    walkthrough: 'GROUP BY city собирает пользователей каждого города вместе; COUNT(*) считает строки в каждой такой группе.',
    practice: 'Если нужен один итог — используйте функцию без GROUP BY. Если итог нужен «для каждого» значения — добавьте GROUP BY этого столбца.'
  },
  {
    intro: 'WHERE проверяет отдельные строки до подсчёта. HAVING проверяет готовые группы после GROUP BY. Это позволяет оставить, например, города с несколькими жителями.',
    steps: ['Сначала выберите столбец группы и агрегат: city, COUNT(*).', 'Затем сгруппируйте данные через GROUP BY city.', 'После группировки задайте условие HAVING COUNT(*) > 1.'],
    pattern: 'SELECT группа, COUNT(*) AS total\nFROM таблица\nGROUP BY группа\nHAVING COUNT(*) > число;',
    walkthrough: 'В примере каждый город превращается в одну группу. HAVING оставляет только группы, где COUNT(*) больше одного.',
    practice: 'Спросите себя, проверяется ли отдельная запись или уже посчитанная группа. Для группы условие пишется после HAVING.'
  },
  {
    intro: 'Данные о покупателях и заказах лежат в разных таблицах. JOIN связывает строки по общим ключам: users.id — номер пользователя, orders.user_id — этот номер в заказе.',
    steps: ['Начните с основной таблицы после FROM.', 'Добавьте JOIN второй таблицы и условие связи после ON.', 'Если названия столбцов совпадают, укажите таблицу: users.name. LEFT JOIN сохраняет строки левой таблицы даже без пары.'],
    pattern: 'SELECT users.name, orders.total\nFROM users\nJOIN orders ON users.id = orders.user_id;',
    walkthrough: 'Каждый заказ ищет пользователя с таким же id. INNER JOIN оставляет только совпадения; LEFT JOIN также покажет пользователей без заказа, с NULL в столбцах orders.',
    practice: 'Найдите таблицы в задании и пару ключей, связывающих их. Решите, нужно ли сохранить строки без совпадения: тогда выбирайте LEFT JOIN.'
  },
  {
    intro: 'Подзапрос — это SELECT внутри другого запроса. Внутренний запрос вычисляет список или одно значение, которым пользуется внешний.',
    steps: ['Сначала мысленно выполните внутренний SELECT.', 'IN сравнивает значение со списком результатов; EXISTS проверяет, нашлась ли хотя бы одна строка.', 'Если внутренний запрос возвращает одно число, его можно сравнить через >, < или =.'],
    pattern: 'SELECT столбцы\nFROM таблица\nWHERE id IN (SELECT другой_id FROM другая_таблица);',
    walkthrough: 'В примере внутренний SELECT получает user_id из orders; внешний SELECT оставляет пользователей, чей id есть в полученном списке.',
    practice: 'Сформулируйте внутренний вопрос отдельно. Проверьте, что он возвращает именно тот столбец или одно значение, с которым сравнивает внешний запрос.'
  },
  {
    intro: 'CASE создаёт вычисляемое значение для каждой строки. Это похоже на «если — то — иначе» и не меняет данные в таблице.',
    steps: ['После CASE WHEN напишите проверяемое условие.', 'После THEN укажите значение при истинном условии; после ELSE — при ложном.', 'Закройте выражение END и дайте новому столбцу имя через AS.'],
    pattern: "SELECT name,\n  CASE WHEN условие THEN 'да' ELSE 'нет' END AS status\nFROM таблица;",
    walkthrough: 'В примере для каждого пользователя проверяется age >= 18. Результат получает имя status, поэтому его можно увидеть как отдельный столбец.',
    practice: 'Выпишите оба варианта результата из задания и условие перехода между ними. Подставьте их в CASE WHEN … THEN … ELSE … END.'
  },
  {
    intro: 'SQL-функция преобразует значение в каждой строке. Исходная таблица при обычном SELECT не меняется.',
    steps: ['UPPER(text) переводит текст в верхний регистр; LENGTH(text) считает символы.', 'date(created_at) оставляет календарную дату.', 'Через AS задайте понятное имя вычисленному столбцу.'],
    pattern: 'SELECT name, UPPER(city) AS city_upper\nFROM users;',
    walkthrough: 'В примере значение city каждой строки передаётся функции UPPER; результат показывается под именем city_upper.',
    practice: 'Определите исходный столбец, нужное преобразование и имя нового столбца. Запишите функцию прямо в SELECT.'
  },
  {
    intro: 'Для сложного анализа можно сначала создать временный результат через WITH, а затем запросить его. Оконные функции считают показатели по строкам без сворачивания строк в группы.',
    steps: ['WITH имя AS (SELECT …) создаёт именованный промежуточный результат для одного запроса.', 'ROW_NUMBER() OVER (PARTITION BY группа ORDER BY значение DESC) нумерует строки внутри группы.', 'Оконный результат можно отобрать внешним SELECT из CTE.'],
    pattern: 'WITH ranked AS (\n  SELECT name, ROW_NUMBER() OVER (ORDER BY price DESC) AS pos\n  FROM products\n)\nSELECT name FROM ranked WHERE pos <= 3;',
    walkthrough: 'Сначала ranked нумерует товары по цене, затем внешний запрос оставляет нужные позиции. Промежуточная таблица существует только во время запроса.',
    practice: 'Разделите задачу на два шага: вычислить показатель во внутреннем запросе и отобрать результат во внешнем.'
  },
  {
    intro: 'NULL означает, что значение отсутствует или неизвестно. Оно отличается от нуля и пустой строки. Обычное сравнение = NULL не работает.',
    steps: ['Для поиска отсутствующих значений используйте IS NULL, для заполненных — IS NOT NULL.', 'COALESCE(rating, 0) подставляет 0, если rating равен NULL.', 'DISTINCT убирает повторяющиеся строки результата; COUNT(столбец) не считает NULL.'],
    pattern: 'SELECT COALESCE(rating, 0) AS score\nFROM reviews;',
    walkthrough: 'Если у отзыва есть rating, COALESCE вернёт его. Если rating отсутствует, результатом будет 0; сама таблица при этом не меняется.',
    practice: 'Проверьте, нужен ли поиск пустых значений, замена при выводе или удаление повторов. Это три разные операции.'
  },
  {
    intro: 'Операторы множеств соединяют результаты двух SELECT. Оба запроса должны возвращать одинаковое число столбцов совместимых типов.',
    steps: ['UNION объединяет результаты и удаляет повторы.', 'UNION ALL сохраняет повторы.', 'INTERSECT оставляет общее, EXCEPT — строки первого запроса, которых нет во втором.'],
    pattern: 'SELECT city FROM users\nUNION\nSELECT city FROM departments;',
    walkthrough: 'Два SELECT возвращают по одному столбцу city. UNION делает единый список городов без повторов.',
    practice: 'Сначала составьте оба SELECT по отдельности. Затем выберите оператор по словам «без повторов», «все записи», «общие» или «только в первой таблице».'
  },
  {
    intro: 'Команды записи меняют данные. В тренажёре каждый запуск происходит на новой учебной копии базы, поэтому можно спокойно экспериментировать.',
    steps: ['INSERT INTO таблица (столбцы) VALUES (значения) добавляет строку.', 'UPDATE таблица SET столбец = значение WHERE условие меняет существующие строки.', 'DELETE FROM таблица WHERE условие удаляет строки. Без WHERE UPDATE и DELETE затронут все строки.'],
    pattern: "UPDATE products\nSET price = 100\nWHERE id = 1;",
    walkthrough: 'Пример ищет товар с id = 1 и меняет только его price. WHERE определяет, какие строки затрагивает команда.',
    practice: 'Уточните вид действия: добавить, изменить или удалить. Для изменения и удаления отдельно запишите условие, определяющее нужные строки.'
  },
  {
    intro: 'CREATE TABLE описывает структуру новой таблицы: её имя, столбцы, типы данных и ограничения. В учебной базе новые таблицы создаются заново при каждом запуске.',
    steps: ['Укажите имя таблицы и столбцы в скобках.', 'INTEGER хранит целые числа, TEXT — текст, REAL — дробные числа.', 'PRIMARY KEY задаёт уникальный ключ; NOT NULL запрещает пустое значение; DEFAULT задаёт значение по умолчанию.'],
    pattern: 'CREATE TABLE notes (\n  id INTEGER PRIMARY KEY,\n  title TEXT NOT NULL,\n  created_at TEXT DEFAULT CURRENT_DATE\n);',
    walkthrough: 'У каждой заметки будет уникальный id. title обязателен, а created_at получит дату по умолчанию, если её не передать.',
    practice: 'Выпишите имя таблицы и все требуемые столбцы. Для каждого найдите тип и ограничения, затем запишите определение между скобками.'
  },
  {
    intro: 'В финальных задачах сочетаются знакомые части SQL. Не пишите весь запрос сразу: разбейте условие на таблицы, связи, фильтры, вычисления и порядок.',
    steps: ['Определите, какие столбцы должен содержать итог.', 'Соберите исходные данные через FROM и JOIN или временный результат WITH.', 'Добавьте WHERE, GROUP BY, HAVING, оконную функцию и ORDER BY только если они нужны задаче.'],
    pattern: 'WITH source AS (\n  SELECT ... FROM ...\n)\nSELECT ...\nFROM source\nWHERE ...;',
    walkthrough: 'Вложенные части проверяйте по одной: сначала запустите исходный SELECT, затем добавьте расчёт и только после этого итоговый отбор.',
    practice: 'Составьте черновой план запроса словами. Запустите промежуточный вариант кнопкой «Только запустить», посмотрите строки и затем доведите до требуемого результата.'
  }
];
